<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cardBoardSQL">	
	<insert id="regist" parameterType="java.util.Map">
		insert into cardboard(seq,title,topic,location,people,content) 
		values((select *from (select max(seq)+1 from cardboard) next),#{title},#{topic},#{location},#{people},#{content})
	</insert>
	<select id="getCardBoardList" resultType="card">
		select * from cardboard order by seq desc
	</select>
	<select id="getLocationList" parameterType="String" resultType="String">
		select substring_index(location,' ',-1) from addr where location like concat('%',#{location},'%')

	</select>

</mapper>